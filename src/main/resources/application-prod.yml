server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://your-prod-server:3306/shixun2?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:your-password}

logging:
  level:
    com.tourism: INFO
    root: WARN
